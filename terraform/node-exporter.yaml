apiVersion: v1
kind: Service
metadata:
  name: node-exporter
  namespace: monitoring
  labels:
    app: prometheus
    component: node-exporter
spec:
  selector:
    app: prometheus
    component: node-exporter
  ports:
    - protocol: TCP
      port: 9100
      targetPort: 9100
  clusterIP: None
